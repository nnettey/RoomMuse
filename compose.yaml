name: roommuse

services:
  studio:
    build:
      context: .
      dockerfile: Dockerfile
    image: roommuse/studio:local
    container_name: roommuse-studio
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      PORT: 3201
      ROOMMUSE_DATA_DIR: /var/lib/roommuse
    ports:
      - "3201:3201"
    volumes:
      - roommuse_data:/var/lib/roommuse
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3201/health"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  roommuse_data:
    name: roommuse_data
